#include <stdio.h>
//arithmatic operators
int main()
{
    int + float = a,b;   // This is a variable declaration
    a = 34;
    b = 6;

    printf("Hello World");
    printf("a + b =%f\n", a+b); // This is a function to print
    printf("a - b =%f\n", a-b); // This is a function to print
    printf("a / b =%f\n", a/b); // This is a function to print
    printf("a % b =%f\n", a%b); // This is a function to print
    return 0;
}


//relational operaters
int main()
{
    int  = a,b;   // This is a variable declaration
    a = 34;
    b = 6;

    printf("Hello World");
    printf("a == b =%f\n", a==b); // This is a function to print
    printf("a => b =%f\n", a=>b); // This is a function to print
    printf("a =< b =%f\n", a=<b); // This is a function to print
    printf("a != b =%f\n", a!=b); // This is a function to print
    printf("a > b =%f\n", a>b); // This is a function to print
    printf("a < b =%f\n", a<b); // This is a function to print
    return 0;
}


//Logical operators
int main()
{
    int  = a,b;   // This is a variable declaration
    a = 34;
    b = 6;

    printf("Hello World");
    printf("a && b =%f\n", a&&b); // This is a function to print
    printf("a || b =%f\n", a||b); // This is a function to print
    printf("!a =%f\n", !a); // This is a function to print
    printf("!b =%f\n", !b); // This is a function to print
    return 0;
}

//Bitwise Operators
int main(
    int = a,b;    // This is a variable declaration in binary
    a = 34;
    b = 6;

    printf("a & b =%f\n", a&b); 
    printf("a | b =%f\n", a|b); 
    printf("a ^ b =%f\n", a^b); 
    return 0;
)

//Assignment operators
int main()
{
    int  = a,b;   // This is a variable declaration
    a = 34;
    b = 6;

    printf("Hello World");
    printf("a += b =%f\n", a+=b); // This is a function to print
    printf("a -= b =%f\n", a-=b); // This is a function to print
    printf("a *= b =%f\n", a*=b); // This is a function to print
    printf("a /= b =%f\n", a/=b); // This is a function to print
    printf("a %= b =%f\n", a%=b); // This is a function to print
    return 0;
}


//increment and decrement operators
int main()
{
    int  = a,b;   // This is a variable declaration
    a = 34;
    b = 6;

    printf("Hello World");
    printf("a++ =%f\n", a++); // This is a function to print
    printf("a-- =%f\n", a--); // This is a function to print
    printf("++a =%f\n", ++a); // This is a function to print
    printf("--a =%f\n", --a); // This is a function to print
    return 0;
}


//conditional operators
int main()
{
    int  = a,b;   // This is a variable declaration
    a = 34;
    b = 6;

    printf("Hello World");
    printf("a == b ? 4 : 6 =%f\n", a==b ? 4 : 6); // This is a function to print
    printf("a > b ? 4 : 6 =%f\n", a>b ? 4 : 6); // This is a function to print
    return 0;
}

//miscellaneous operators
int main(
    a = 34;
    b = 6;
     
    printf(sizeof(a));
    printf(&a);
    printf(*a);
    printf(?:a)
)