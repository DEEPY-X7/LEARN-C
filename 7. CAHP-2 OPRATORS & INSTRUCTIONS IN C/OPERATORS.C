/*
int b = 2,  c=3;
intz; z = b*c;        //its allowed
int z = b * c =z;    //Notallowed

% →  Modular division operator
% →  Returns the remaindez
% →  cannot be applied on float
% → Sige is same as of niverator (-5/20 =-1)
 

INT  = SAVE KARTA HAI DIRECT NUMBER KO 
FLOAT  = MEIN DECIMAL POINT BHI SAVE KARTA HAI

>>>>OPERATOR PRECEDENCE IN C
1. Pariority of 
 x * y/z => x * (y/z)
 y/z * x => (y/z) * x
 z * x/y => z * (x/y)

2. Associativity of operators
    x * y * z => (x * y) * z
    x / y / z => (x / y) / z
    x % y % z => (x % y) % z

    
control instructions
1. sequence control instruction
2. decision control instruction
3. loop control instruction
4. case control instruction
etc.
*/